// export default function callApi (url, options) {
//     return wretch(url, options).then((response => {
//         if(!response.ok) {
//             throw Error('Houston we have a problem!')
//         }
//         return response;
//     }))
// }